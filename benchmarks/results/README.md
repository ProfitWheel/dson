# Benchmark Results

This directory contains all benchmark run outputs:
- `*.log` - Detailed execution logs
- `*.csv` - Result data in CSV format
- `benchmarks.db` - Stores all benchmarks runs in a sqlite database
- `summary_%timestamp%.txt` - Summary of the benchmark run

